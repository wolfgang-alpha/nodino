# TODO

## Done

- Speech-to-text via faster-whisper (medium.en model, int8 quantization)
- Mempalace integration (semantic search + knowledge graph)
- Mempalace MCP server for tool-use access
- CalDAV integration via MCP
- Claude Code mempalace bridge (user-level MCP config)
- Conversation mining (mine-sessions.py, 101 drawers extracted)
- Standalone mempalace deployment (decoupled from nodino)
- Voice capture app (ccgo) for async voice-to-knowledge processing

## Future

- Strip backend to pure CRUD (remove claude -p session logic)
- Mobile-optimized UI (responsive layout for small screens)
- Multi-language STT support (currently English only)
- Project file mining — import documents/notes into the memory palace
